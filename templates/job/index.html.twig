{% extends './base.html.twig' %}

{% block body%}
    
<vue-job-list
    api-url="{{ listing_apiUrl }}"
    detail-url="{{ listing_detailUrl }}"
    history-url="{{ listing_historyUrl }}"
    page="{{ listing_page }}"
    cachedResponse="{{ listing_cache }}"
></vue-job-list>
    
{% endblock %}
    